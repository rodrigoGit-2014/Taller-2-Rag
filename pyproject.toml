[project]
name = "taller-2-rag"
version = "0.1.0"
description = "Taller 2 · RAG paso a paso con LangChain, Redis y LangGraph (Gemini)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain-core>=1.6.4",
    "langchain-community>=0.4.2",
    "langchain-google-genai>=4.4.0",
    "langchain-text-splitters>=1.1.2",
    "langgraph>=1.2.12",
    "pypdf>=6.19.0",
    "python-dotenv>=1.2.3",
    "redis>=8.1.0",
]

[tool.uv]
package = false
